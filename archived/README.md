# archived

Retired packages kept for reference only. Nothing in active `packages/*` imports from here — do not add new imports.

- `adapters` — deprecated on npm
- `cli` — old 0.6.x CLI, deprecated on npm (replaced by `packages/cli` 0.7.x)
- `registry` — never published
- `json-render` — never published
- `playground` — never published
