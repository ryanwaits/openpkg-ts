---
"@openpkg-ts/spec": minor
"@openpkg-ts/sdk": minor
"@openpkg-ts/cli": minor
---

Resolve a TypeScript package and entry from a directory, cwd, or intent instead of requiring an explicit entry file. Specs now record `generation.entryPoint` and `generation.entryPointSource`.
