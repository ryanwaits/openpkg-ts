---
"@openpkg-ts/cli": minor
---

Ship the generate-docs agent skill in the npm tarball (skills/generate-docs/SKILL.md). Copy it into your project's .claude/skills/ to let an agent scaffold framework-ready API reference docs (Fumadocs, Docusaurus, plain Markdown) with navigation, search indexes, and output verification.
